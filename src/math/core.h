#ifndef CORE_H
#define CORE_H


#include <cstdlib>
#include <vector>

using std::vector;

int check_broadcast(vector<int> &a, vector<int> &b);
vector<int> broadcast(vector<int> &a, vector<int> &b);

#endif